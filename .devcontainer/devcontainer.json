{
	"name": "Marlowe Enhancement",
    // Use this dockerfile if the image is taking forever to download or something
	"dockerFile": "./Dockerfile",
	"settings": {
		"terminal.integrated.shell.linux": "/bin/bash",
	},
	"extensions": [
		"nwolverson.language-purescript",
		"justusadam.language-haskell"
	],
    "forwardPorts": [ 8080, 8009, 8081 ],

    // Allow nix to access this
    "postCreateCommand": "chmod 777 /workspaces/plutus-prototype/.git/refs/heads/wyohack"
}